import { Schema, ResolvedPos } from 'prosemirror-model';
export declare const isWrappingPossible: (nodeType: any, state: any) => boolean;
export declare const getListLiftTarget: (schema: Schema<any, any>, resPos: ResolvedPos<any>) => number;
